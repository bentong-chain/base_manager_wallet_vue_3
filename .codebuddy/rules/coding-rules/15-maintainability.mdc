---
description: Vue 3 可维护性规范
alwaysApply: true
enabled: true
---

# 可维护性规范

## 代码可读性

### 1. 使用有意义的命名
```typescript
✅ 正确
const userList = ref<User[]>([])
const isLoading = ref(false)
const fetchUserData = async () => {}

❌ 错误
const list = ref([])
const flag = ref(false)
const getData = async () => {}
```

### 2. 避免魔法数字
```typescript
✅ 正确
const MAX_RETRY_COUNT = 3
const DEFAULT_TIMEOUT = 5000

if (retryCount >= MAX_RETRY_COUNT) {
  // 处理
}

❌ 错误
if (retryCount >= 3) {
  // 3 是什么意思？
}
```

### 3. 函数保持简短
```typescript
✅ 正确 - 单一职责
function validateEmail(email: string): boolean {
  return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)
}

function validatePhone(phone: string): boolean {
  return /^1[3-9]\d{9}$/.test(phone)
}

❌ 错误 - 职责过多
function validate(type: string, value: string): boolean {
  if (type === 'email') {
    return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value)
  } else if (type === 'phone') {
    return /^1[3-9]\d{9}$/.test(value)
  }
  // ...更多验证逻辑
}
```

## 代码组织

### 1. 模块化设计
```
src/
├── api/              # API 层
├── components/       # 组件层
├── composables/      # 逻辑层
├── stores/           # 状态层
├── utils/            # 工具层
└── views/            # 页面层
```

### 2. 关注点分离
```vue
<!-- ✅ 正确 - 逻辑分离到 composable -->
<script setup lang="ts">
import { useUserList } from '@/composables/useUserList'

const { users, loading, fetchUsers, deleteUser } = useUserList()

onMounted(() => {
  fetchUsers()
})
</script>

<!-- ❌ 错误 - 所有逻辑在组件中 -->
<script setup lang="ts">
const users = ref<User[]>([])
const loading = ref(false)

async function fetchUsers() {
  loading.value = true
  try {
    users.value = await get<User[]>('/users')
  } finally {
    loading.value = false
  }
}

async function deleteUser(id: number) {
  // 删除逻辑
}
</script>
```

### 3. 配置集中管理
```typescript
// config/index.ts
export const config = {
  api: {
    baseURL: import.meta.env.VITE_API_BASE_URL,
    timeout: 5000
  },
  pagination: {
    defaultPageSize: 20,
    pageSizes: [10, 20, 50, 100]
  },
  upload: {
    maxSize: 5 * 1024 * 1024, // 5MB
    allowedTypes: ['image/jpeg', 'image/png']
  }
}
```

## 文档编写

### 1. README 文档
```markdown
# 项目名称

## 简介
项目的简要描述

## 技术栈
- Vue 3
- TypeScript
- Pinia
- Element Plus

## 开始使用

### 安装依赖
\`\`\`bash
npm install
\`\`\`

### 开发环境
\`\`\`bash
npm run dev
\`\`\`

### 构建生产
\`\`\`bash
npm run build
\`\`\`

## 项目结构
\`\`\`
src/
├── api/          # API 接口
├── components/   # 组件
├── views/        # 页面
└── ...
\`\`\`

## 贡献指南
请阅读 CONTRIBUTING.md
```

### 2. 组件文档
```vue
<script setup lang="ts">
/**
 * 用户卡片组件
 * 
 * @description
 * 展示用户基本信息的卡片组件，支持点击和编辑操作
 * 
 * @example
 * ```vue
 * <UserCard
 *   :user="userData"
 *   :editable="true"
 *   @click="handleClick"
 *   @edit="handleEdit"
 * />
 * ```
 */

interface Props {
  /** 用户数据 */
  user: User
  /** 是否可编辑 */
  editable?: boolean
}

const emit = defineEmits<{
  /** 点击卡片时触发 */
  'click': [user: User]
  /** 点击编辑按钮时触发 */
  'edit': [user: User]
}>()
</script>
```

### 3. API 文档
```typescript
/**
 * 获取用户列表
 * 
 * @param params - 查询参数
 * @param params.page - 页码，从 1 开始
 * @param params.pageSize - 每页数量
 * @param params.keyword - 搜索关键词（可选）
 * @returns 用户列表和总数
 * 
 * @example
 * ```typescript
 * const result = await getUserList({ page: 1, pageSize: 20 })
 * console.log(result.list, result.total)
 * ```
 */
export async function getUserList(params: {
  page: number
  pageSize: number
  keyword?: string
}): Promise<{ list: User[]; total: number }> {
  return await get('/users', params)
}
```

## 代码重构

### 1. 识别重构时机
- 代码重复
- 函数过长
- 类/组件过大
- 命名不清晰
- 逻辑复杂

### 2. 重构步骤
```typescript
// 步骤 1: 识别问题
// ❌ 重复代码
function processUserA(user: User) {
  if (!user.email) return
  const formatted = user.email.toLowerCase().trim()
  // 处理逻辑
}

function processUserB(user: User) {
  if (!user.email) return
  const formatted = user.email.toLowerCase().trim()
  // 处理逻辑
}

// 步骤 2: 提取公共逻辑
function formatEmail(email: string): string {
  return email.toLowerCase().trim()
}

// 步骤 3: 重构
function processUserA(user: User) {
  if (!user.email) return
  const formatted = formatEmail(user.email)
  // 处理逻辑
}

function processUserB(user: User) {
  if (!user.email) return
  const formatted = formatEmail(user.email)
  // 处理逻辑
}
```

### 3. 重构原则
- 小步快跑
- 保持功能不变
- 及时测试
- 提交记录清晰

## 版本管理

### 1. 语义化版本
```
主版本号.次版本号.修订号

1.0.0 -> 1.0.1 (修复 bug)
1.0.1 -> 1.1.0 (新增功能)
1.1.0 -> 2.0.0 (破坏性更新)
```

### 2. 变更日志
```markdown
# Changelog

## [1.1.0] - 2024-03-09

### Added
- 新增用户管理功能
- 新增数据导出功能

### Changed
- 优化登录流程
- 更新 UI 样式

### Fixed
- 修复分页 bug
- 修复表单验证问题

### Deprecated
- 废弃旧版 API

### Removed
- 移除无用代码

### Security
- 修复 XSS 漏洞
```

## 依赖管理

### 1. 定期更新依赖
```bash
# 检查过期依赖
npm outdated

# 更新依赖
npm update

# 更新到最新版本
npm install package@latest
```

### 2. 锁定版本
```json
{
  "dependencies": {
    "vue": "^3.4.0",      // 允许小版本更新
    "axios": "~1.6.0",    // 只允许补丁更新
    "lodash": "4.17.21"   // 锁定版本
  }
}
```

## 代码审查

### 1. 审查清单
- [ ] 代码符合规范
- [ ] 命名清晰合理
- [ ] 逻辑正确无误
- [ ] 错误处理完善
- [ ] 性能考虑充分
- [ ] 安全问题排查
- [ ] 测试覆盖充分
- [ ] 文档更新完整

### 2. 审查重点
- 业务逻辑正确性
- 边界条件处理
- 错误处理
- 性能影响
- 安全隐患
- 代码可读性

## 技术债务管理

### 1. 记录技术债务
```typescript
// TODO: 优化性能，考虑使用虚拟滚动
// FIXME: 修复边界情况的 bug
// HACK: 临时方案，需要重构
// NOTE: 这里的逻辑比较复杂，需要仔细理解
```

### 2. 定期清理
- 每个迭代预留时间
- 优先级排序
- 逐步解决

## 总结

- 保持代码可读性
- 模块化组织代码
- 编写完善文档
- 及时重构代码
- 管理好依赖
- 进行代码审查
- 处理技术债务
