---
description: Vue 3 性能优化规范
alwaysApply: true
enabled: true
---

# 性能优化规范

## 组件优化

### 1. 使用 v-show vs v-if
```vue
✅ 频繁切换使用 v-show
<div v-show="isVisible">内容</div>

✅ 条件渲染使用 v-if
<div v-if="hasPermission">内容</div>
```

### 2. 使用 key 优化列表
```vue
✅ 正确
<div v-for="item in items" :key="item.id">
  {{ item.name }}
</div>

❌ 错误
<div v-for="(item, index) in items" :key="index">
  {{ item.name }}
</div>
```

### 3. 计算属性缓存
```typescript
✅ 使用 computed（有缓存）
const filteredList = computed(() => {
  return items.value.filter(item => item.active)
})

❌ 使用方法（无缓存）
function getFilteredList() {
  return items.value.filter(item => item.active)
}
```

### 4. 避免不必要的响应式
```typescript
✅ 正确
const config = { timeout: 5000 }  // 不需要响应式
const count = ref(0)  // 需要响应式

❌ 错误
const config = ref({ timeout: 5000 })  // 不必要的响应式
```

## 路由优化

### 1. 路由懒加载
```typescript
✅ 正确
{
  path: '/user',
  component: () => import('@/views/User.vue')
}

❌ 错误
import User from '@/views/User.vue'
{
  path: '/user',
  component: User
}
```

### 2. 路由预加载
```typescript
// 鼠标悬停时预加载
<router-link
  to="/user"
  @mouseenter="() => import('@/views/User.vue')"
>
  用户
</router-link>
```

## 资源优化

### 1. 图片优化
```vue
<!-- 使用合适的图片格式 -->
<img src="@/assets/logo.webp" alt="Logo" />

<!-- 响应式图片 -->
<img
  srcset="
    @/assets/logo-small.jpg 480w,
    @/assets/logo-medium.jpg 800w,
    @/assets/logo-large.jpg 1200w
  "
  sizes="(max-width: 600px) 480px, (max-width: 1000px) 800px, 1200px"
  src="@/assets/logo-medium.jpg"
  alt="Logo"
/>

<!-- 懒加载图片 -->
<img src="@/assets/placeholder.jpg" data-src="@/assets/real-image.jpg" loading="lazy" />
```

### 2. 代码分割
```typescript
// 按路由分割
const routes = [
  {
    path: '/admin',
    component: () => import(/* webpackChunkName: "admin" */ '@/views/Admin.vue')
  }
]

// 按功能分割
const HeavyComponent = defineAsyncComponent(() =>
  import('@/components/HeavyComponent.vue')
)
```

## 列表优化

### 1. 虚拟滚动
```vue
<script setup lang="ts">
import { VirtualScroller } from 'vue-virtual-scroller'
import 'vue-virtual-scroller/dist/vue-virtual-scroller.css'

const items = ref(Array.from({ length: 10000 }, (_, i) => ({
  id: i,
  name: `Item ${i}`
})))
</script>

<template>
  <VirtualScroller
    :items="items"
    :item-size="50"
    key-field="id"
  >
    <template #default="{ item }">
      <div class="item">{{ item.name }}</div>
    </template>
  </VirtualScroller>
</template>
```

### 2. 分页加载
```typescript
const page = ref(1)
const pageSize = 20
const items = ref<Item[]>([])

async function loadMore() {
  const newItems = await fetchItems(page.value, pageSize)
  items.value.push(...newItems)
  page.value++
}
```

## 请求优化

### 1. 防抖和节流
```typescript
import { debounce } from 'lodash-es'

// 防抖：搜索输入
const searchUsers = debounce(async (keyword: string) => {
  const users = await get<User[]>('/users/search', { keyword })
  userList.value = users
}, 300)

// 节流：滚动事件
const handleScroll = throttle(() => {
  console.log('Scrolling...')
}, 200)
```

### 2. 请求缓存
```typescript
const cache = new Map<string, any>()

async function fetchWithCache<T>(url: string): Promise<T> {
  if (cache.has(url)) {
    return cache.get(url)
  }
  
  const data = await get<T>(url)
  cache.set(url, data)
  
  // 5分钟后清除缓存
  setTimeout(() => cache.delete(url), 5 * 60 * 1000)
  
  return data
}
```

### 3. 并发请求
```typescript
// 并发执行多个请求
const [users, products, orders] = await Promise.all([
  get<User[]>('/users'),
  get<Product[]>('/products'),
  get<Order[]>('/orders')
])
```

## 渲染优化

### 1. 使用 v-once
```vue
<!-- 只渲染一次，不再更新 -->
<div v-once>{{ staticContent }}</div>
```

### 2. 使用 v-memo
```vue
<!-- 仅在依赖项变化时更新 -->
<div v-memo="[item.id, item.name]">
  {{ item.name }}
</div>
```

### 3. 异步组件
```typescript
const AsyncComponent = defineAsyncComponent({
  loader: () => import('@/components/Heavy.vue'),
  loadingComponent: LoadingSpinner,
  errorComponent: ErrorDisplay,
  delay: 200,
  timeout: 3000
})
```

## 打包优化

### 1. Vite 配置优化
```typescript
// vite.config.ts
export default defineConfig({
  build: {
    // 代码分割
    rollupOptions: {
      output: {
        manualChunks: {
          'vue-vendor': ['vue', 'vue-router', 'pinia'],
          'element-plus': ['element-plus'],
          'utils': ['lodash-es', 'axios']
        }
      }
    },
    // 压缩
    minify: 'terser',
    terserOptions: {
      compress: {
        drop_console: true,
        drop_debugger: true
      }
    }
  }
})
```

### 2. Tree Shaking
```typescript
✅ 正确 - 按需导入
import { ref, computed } from 'vue'
import { ElMessage } from 'element-plus'

❌ 错误 - 全量导入
import * as Vue from 'vue'
import ElementPlus from 'element-plus'
```

## 状态管理优化

### 1. 避免过度使用全局状态
```typescript
✅ 正确 - 局部状态
const count = ref(0)

❌ 错误 - 不必要的全局状态
const store = useCountStore()
```

### 2. 选择性订阅
```typescript
// 只订阅需要的状态
const { user, isLoggedIn } = storeToRefs(useAuthStore())

// 不要订阅整个 store
const authStore = storeToRefs(useAuthStore())
```

## 内存优化

### 1. 清理定时器
```typescript
let timer: number | undefined

onMounted(() => {
  timer = window.setInterval(() => {
    // 定时任务
  }, 1000)
})

onUnmounted(() => {
  if (timer) {
    clearInterval(timer)
  }
})
```

### 2. 移除事件监听
```typescript
function handleResize() {
  // 处理逻辑
}

onMounted(() => {
  window.addEventListener('resize', handleResize)
})

onUnmounted(() => {
  window.removeEventListener('resize', handleResize)
})
```

### 3. 取消未完成的请求
```typescript
const controller = new AbortController()

async function fetchData() {
  try {
    const data = await fetch('/api/data', {
      signal: controller.signal
    })
    return data
  } catch (error) {
    if (error.name === 'AbortError') {
      console.log('请求已取消')
    }
  }
}

onUnmounted(() => {
  controller.abort()
})
```

## 性能监控

### 1. 性能指标
```typescript
// 监控首屏加载时间
window.addEventListener('load', () => {
  const perfData = performance.timing
  const loadTime = perfData.loadEventEnd - perfData.navigationStart
  console.log('页面加载时间:', loadTime, 'ms')
})
```

### 2. 组件渲染时间
```typescript
onMounted(() => {
  const start = performance.now()
  
  // 组件逻辑
  
  const end = performance.now()
  console.log('组件渲染时间:', end - start, 'ms')
})
```

## 总结

- 合理使用 v-show 和 v-if
- 路由和组件懒加载
- 虚拟滚动处理大列表
- 防抖节流优化请求
- 请求缓存和并发
- 代码分割和 Tree Shaking
- 及时清理资源
- 监控性能指标
